#ifndef CHW_TOOLS_H
#define CHW_TOOLS_H

#include <iostream>
#include <vector>

void is_sorted(std::vector<int> &vector);
std::ostream& operator <<(std::ostream &os, const std::vector<int>& vector);

#endif
